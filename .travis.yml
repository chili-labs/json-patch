language: php
php:
    - 5.3
    - 5.4
    - 5.5
    - 5.6
    - hhvm
before_install:
     - sudo pip install codecov
install:
    - composer install --prefer-source
script:
    - phpunit --coverage-clover build/reports/clover.xml
after_success:
    - codecov
